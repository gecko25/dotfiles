# ruby/rails
alias bes="bundle exec rspec"
alias ber="bundle exec rake"
alias bake="bundle exec rake"
alias be="bundle exec"
alias bej="bundle exec guard-jasmine spec"


# git
alias g="git"
alias gbr="git remote prune origin && git branch -r"

# filesystem
alias ..="cd ../"
alias ...="cd ../.."
alias ....="cd ../../.."
alias o=$EDITOR
alias dotf="cd $HOME/.dotfiles"
alias ls="ls -G"
alias mkdir="mkdir -p"

# misc.
alias timestamp="date +%Y%m%d%H%M%S"


alias BETASERON_SOURCE="cd ~/Documents/repositories/betaseron-website/src/main/webapp/source"
alias BETASERON="cd ~/Documents/repositories/betaseron-website"

#Make our terminal colorful
#source ~/.bashrc
alias ls="ls -GFlash"
